# Otto Configuration
# Run ./otto configure to set up interactively, or edit this file directly.

# ---------------------------------------------------------------------------
# Core (set automatically — you shouldn't need to change these)
# ---------------------------------------------------------------------------
DEPLOYMENT_TYPE=onprem_frontend
FRONTEND_URL=http://localhost:3000
BACKEND_API_URL=http://localhost:8000
DATABASE_URL=sqlite:///./otto.db
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./sandbox
DEMO_MODE=false

# ---------------------------------------------------------------------------
# LLM Provider (required — pick one)
# ---------------------------------------------------------------------------

# Option 1: Google Gemini (recommended)
USE_GEMINI=true
GEMINI_MODEL=gemini-2.5-pro-preview-05-06
GOOGLE_API_KEY=

# Option 2: OpenAI
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Option 3: Ollama (local, no API key needed)
USE_OLLAMA=false
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.2

# Embedding model (optional — auto-selected from your LLM provider)
# EMBEDDING_MODEL=models/gemini-embedding-001  # Google default
# EMBEDDING_MODEL=text-embedding-3-small      # OpenAI default

# ---------------------------------------------------------------------------
# Web Search (recommended — gives Otto internet access)
# ---------------------------------------------------------------------------
# Get a free key at https://tavily.com (1,000 searches/month)
# TAVILY_API_KEY=

# ---------------------------------------------------------------------------
# Otto Personality (optional)
# ---------------------------------------------------------------------------
# SPECIALIZATION=
# ADDITIONAL_INSTRUCTIONS=

# ---------------------------------------------------------------------------
# Integrations (optional)
# ---------------------------------------------------------------------------

# Slack
# SLACK_BOT_TOKEN=
# SLACK_SIGNING_SECRET=
# SLACK_MCP_XOXB_TOKEN=

# Email (SMTP for sending, IMAP for receiving)
# SMTP_HOST=
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASS=
# IMAP_HOST=
# IMAP_PORT=993
# IMAP_CHECK=30

# ---------------------------------------------------------------------------
# Security (optional, recommended for production)
# ---------------------------------------------------------------------------
# REDIS_PASSWORD=

# ---------------------------------------------------------------------------
# Observability (optional)
# ---------------------------------------------------------------------------
LANGCHAIN_TRACING_V2=false
LANGSMITH_TRACING=false
# LANGCHAIN_API_KEY=
# LANGCHAIN_PROJECT=

# ---------------------------------------------------------------------------
# Advanced
# ---------------------------------------------------------------------------
# CLOUDFLARE_TUNNEL_TOKEN=  # Remote access via Cloudflare tunnel
# BACKEND_IMAGE_TAG=latest  # Pin backend image version
# FRONTEND_IMAGE_TAG=latest # Pin frontend image version
